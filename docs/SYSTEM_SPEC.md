# System Specification

This document has moved to the `opx-strategy` repository.

See `opx-strategy/docs/SYSTEM_OVERVIEW.md` for the current version.

`opx` is scoped to collecting, normalizing, exporting, and inspecting option-chain
data. The decision engine specification belongs in `opx-strategy`.
