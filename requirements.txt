# yfinance          - Fetches real-time/delayed options chains and underlying prices from Yahoo Finance
#                    Required: stock.options, option_chain(), Ticker.info['regularMarketPrice']
yfinance>=0.2.40

# pandas            - Handles all data as DataFrames: combining chains, filtering, renaming columns,
#                    numeric conversion, sorting, and CSV export
pandas>=2.0

# numpy             - Used inside Black-Scholes calculations (np.log, np.sqrt, np.exp, array ops)
numpy>=1.24

# scipy             - Provides norm.cdf (cumulative distribution) and norm.pdf (density function)
#                    → essential for delta, gamma, vega, theta calculations
scipy>=1.10

# playwright        - Headless browser automation for UI screenshots and browser smoke tests
#                    After install, run: python3 -m playwright install
playwright>=1.53

# pytest            - Fast unit tests for metrics, filters, and viewer payload helpers
pytest>=8.0

# massive           - Official Massive / Polygon client library for options snapshot access
massive>=2.4.0

# marketdata-sdk-py - Official Market Data client library for options chains and stock quotes
marketdata-sdk-py>=1.2.0

# tomli             - Backfill for tomllib when CI or local environments use Python < 3.11
tomli>=2.0; python_version < "3.11"
